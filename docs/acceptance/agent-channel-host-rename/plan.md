# agent-channel-host 重命名验收计划

## 范围

- npm/GitHub/CLI/状态目录/env/计划任务/App Server identity/日志视图标识；
- README、示例、当前规格和可复用 canary；
- 旧环境变量 fail-fast 与 SQLite/runtime 标识不变；
- 现有 PR、双平台 CI、GitHub 仓库名、origin 和本地目录回查。

本轮不连接真实 DWS、不发送钉钉消息、不安装或迁移 Windows 计划任务、不发布 npm registry package。
